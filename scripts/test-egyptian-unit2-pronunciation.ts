// @ts-nocheck
import assert from 'node:assert/strict';for(const extension of ['.mp3','.png','.jpg','.jpeg','.webp'])require.extensions[extension]=(module:NodeModule,filename:string)=>{module.exports=filename;};process.env.EXPO_PUBLIC_APP_ENV='preview';process.env.EXPO_PUBLIC_EGYPTIAN_UNIT2_CURRICULUM_VERSION='v2';const {buildEgyptianUnit2CurriculumUnit}=require('../data/curriculum/egyptian');const {getDialectContent}=require('../data/content-registry');const {resolveCurriculumItem}=require('../utils/content-resolver');const unit=buildEgyptianUnit2CurriculumUnit('v2');const content=getDialectContent('egyptian');unit.items.forEach((item,index)=>{const mission=resolveCurriculumItem(item,content).missionContent;if(index<10){assert.equal(mission.audioMode,'default');assert.equal(mission.pronunciationEnabled,true);mission.lessonWords.forEach(word=>assert.equal(word.evalTarget,word.displayArabic));}else{assert.equal(mission.audioMode,index===11?'default':'none');assert.notEqual(mission.pronunciationEnabled,true);}});console.log('Egyptian Unit 2 pronunciation capability test passed.');
